name: Deploy dev

on:
  push:
    branches: [dev]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

concurrency:
  group: deploy-dev
  cancel-in-progress: true

permissions:
  id-token: write        # required for OIDC federation
  contents: read

env:
  ENVIRONMENT: dev
  SWA_NAME: lineup-dev-webapp
  RESOURCE_GROUP: wus3-lineup-dev-rg

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: |
          npm run build --workspace packages/shared
          npm run build --workspace apps/web
          npm run build --workspace api

      # OIDC: GitHub proves its identity per run. No stored client secret.
      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # Fetch the deploy token at runtime so it never lives in GitHub.
      - name: Get SWA deployment token
        id: swa
        run: |
          TOKEN=$(az staticwebapp secrets list \
            --name "$SWA_NAME" \
            --resource-group "$RESOURCE_GROUP" \
            --query "properties.apiKey" -o tsv)
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Deploy to Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.swa.outputs.token }}
          action: upload
          app_location: apps/web/dist
          api_location: api
          skip_app_build: true
          skip_api_build: true

      - name: Smoke test
        run: |
          HOST=$(az staticwebapp show --name "$SWA_NAME" \
            --resource-group "$RESOURCE_GROUP" \
            --query "defaultHostname" -o tsv)
          echo "Deployed to https://$HOST"
          for i in 1 2 3 4 5; do
            CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://$HOST/api/health" || true)
            echo "attempt $i -> $CODE"
            if [ "$CODE" = "200" ] || [ "$CODE" = "503" ]; then exit 0; fi
            sleep 15
          done
          echo "Health endpoint never responded"
          exit 1
